# Web Client — Browser Voice Interface

See `../plans/WEB_PLAN.md` for full architecture.

## Usage
Served automatically by the Pipecat server at `https://your-server:<your-port>/`.

No build step needed — pure HTML/CSS/JS.
